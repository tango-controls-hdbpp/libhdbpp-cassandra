package fr.soleil.mambo;


import junit.framework.TestCase;

public class MamboTest extends TestCase {

	public void testReadProperties() {
		System.out.println("hello");
		// NO MORE USE the HDB Available or not is insert in the command line
		// Read the mambo properties
	/*	Mambo.readProperties();
		boolean hdbAvailable = Mambo.isHdbAvailable();
		
		// 1 - Properties file doesn't exist or the hdb properties is not inside or it is not well written
		assertTrue("Fichier vide ==> hdb available",hdbAvailable == true);
		System.out.println("Mambo.isHdbAvailable() = " + hdbAvailable); 
				
		// 2 - Properties file exists whith HDB = true
		assertTrue("Fichier HDB True ==> hdb available",hdbAvailable == true);
		System.out.println("Mambo.isHdbAvailable() = " + hdbAvailable); 	
	
		// 3 - Properties file exists whith HDB = false
		assertTrue("Fichier HDB False ==> hdb available",hdbAvailable == false);
		System.out.println("Mambo.isHdbAvailable() = " + hdbAvailable); */
		
	}

}
